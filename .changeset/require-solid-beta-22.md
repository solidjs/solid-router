---
"@solidjs/router": patch
---

Require `solid-js` / `@solidjs/web` `2.0.0-beta.22` — the first published beta carrying the element-claim runtime contract (`registerElementClaim`, compiler claim emission) and the self-describing action url support the claims consumer and server-component form delegation build on.
